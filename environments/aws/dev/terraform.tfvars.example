# GLOBALS

environment = "dev"

# A role arn that will be granted access to the cluster
# admin_role_arn = ""


# VPC

# AWS region where the resources will be deployed
region = "us-east-2"
vpc_id = ""


# KUBERNETES

db_instance_type = "t3.micro"
cluster_name = "eyelevel"
cluster_version = "1.30"


# NETWORK

# Control whether to create new network resources
create_subnets = false
create_vpc = false

# The CIDR block that has ingress access to Kubernetes
ingress_cidr = "0.0.0.0/0"

# The subnet IDs for resources, must be set if not creating resources
subnet_ids = ["subnet-xxxxxxxx", "subnet-yyyyyyyy"]


# DATABASE

# The name of the MySQL database
db_name = "eyelevel"

# The username for the MySQL database
db_username = "admin"

# The password for the MySQL database
db_password = "supersecretpassword"